const { UserModel } = require("./User");
const { DiagramModel } = require("./Diagram");
const { NodeModel } = require("./Node");
const { EdgeModel } = require("./Edge");

module.exports = {
  UserModel,
  DiagramModel,
  NodeModel,
  EdgeModel,
};
